# xdg-desktop-portal-hyprland
# allow_token_by_default: autoriza una vez estando delante y luego reconecta
# solo, sin pedir confirmacion en la pantalla. Necesario para RustDesk remoto.
screencopy {
    allow_token_by_default = true
    max_fps = 30
}
