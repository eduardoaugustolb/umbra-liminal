
# #######################################################################################
# AUTOGENERATED HYPRLAND CONFIG.
# EDIT THIS CONFIG ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

autogenerated = 0 # remove this line to remove the warning

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hypr.land/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hypr.land/Configuring/Monitors/
#
# Regla COMODIN a proposito: sirve igual para el portatil (eDP-1) que para la
# torre (DP-4 + DP-6) sin tocar nada. 'preferred' coge el modo nativo de cada
# salida y 'auto' las coloca una a la derecha de otra en el orden en que
# aparecen. Nombres a fuego, nunca: solo funcionarian en una de las dos maquinas.
monitor=,preferred,auto,1

# Para afinar en un equipo concreto se anaden reglas CON NOMBRE debajo de la
# comodin (la ultima que encaja manda). Los nombres salen de `hyprctl monitors`
# y la posicion es la esquina superior izquierda en pixeles ya escalados:
#
#   monitor=DP-4,preferred,0x0,1
#   monitor=DP-6,preferred,2560x0,1


###################
### MY PROGRAMS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = nautilus


#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = $terminal
# exec-once = nm-applet &
# UWSM exporta el entorno de sesión a D-Bus y systemd.
# exec-once = waybar   # sustituida por MenuBar.qml + Notch.qml de Quickshell
exec-once = ~/.config/hypr/awww-start.sh
# Quickshell (barra + notch) lo supervisa quickshell.service: si peta, systemd lo levanta.
# Hypridle y el agente PolicyKit quedan supervisados por sus servicios de usuario.
exec-once = wl-paste --type text --watch cliphist store     # historial de portapapeles (texto)
exec-once = wl-paste --type image --watch cliphist store    # historial de portapapeles (imágenes)



#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hypr.land/Configuring/Environment-variables/
env = XCURSOR_THEME,Bibata-Modern-Ice
env = XCURSOR_SIZE,19
env = HYPRCURSOR_THEME,Bibata-Modern-Ice
env = HYPRCURSOR_SIZE,19

# Espejo de hyprland.lua: hyprcursor APAGADO a proposito. El tema SVG de
# Bibata esta instalado para que el zoom del shake-to-find salga nitido, pero
# si Hyprland lo usa tambien para el cursor NORMAL, ese cursor deja de ser el
# XCursor bitmap de siempre y se nota al momento. El plugin va parcheado para
# cargar el SVG por su cuenta, asi que aqui no hace falta.
# SIN ESTA LINEA, arrancar por este .conf de respaldo cambia el cursor.
cursor {
    enable_hyprcursor = false
}




###################
### PERMISSIONS ###
###################

# See https://wiki.hypr.land/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hypr.land/Configuring/Variables/

# https://wiki.hypr.land/Configuring/Variables/#general
general {
    # Ley 8 — un solo hueco en la capa de contenido. Entre dos ventanas quedan
    # 6 px (3 + 3) y hasta el borde de la pantalla, otros 6: EL MISMO hueco.
    # Antes eran 6 y 5. Un pixel de desajuste no se ve, pero se siente.
    gaps_in = 3
    gaps_out = 6

    # Ley 2 — sin borde, a proposito: el foco se marca con luz, no con
    # contorno. (Aqui vivian los colores teal/verde de fabrica de Hyprland,
    # que contradecian toda la paleta y ademas estaban muertos porque pywal
    # los pisa mas abajo. Fuera.) Si algun dia quieres bordes: sube esto a 2
    # y ya vienen tematizados desde ~/.cache/wal/colors-hyprland.conf.
    border_size = 0

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = true

    # Please see https://wiki.hypr.land/Configuring/Tearing/ before you turn this on
    allow_tearing = true

    layout = dwindle
}

# https://wiki.hypr.land/Configuring/Variables/#decoration
decoration {
    # Ley 1 — la geometria dice de quien es la capa. Las ventanas van RECTAS
    # (esto es tuyo y esta anclado a la rejilla); lo redondo es del shell y
    # flota por encima. No redondear esto "por coherencia": la coherencia la
    # da el movimiento, no la forma.
    rounding = 0
    rounding_power = 0

    # Ley 2 — el foco se marca con luz. La ventana enfocada esta PLENAMENTE
    # presente; las demas se hunden. Antes la activa estaba a 0.98: un velo
    # que no comunicaba nada y que ademas restaba contraste al texto, porque
    # kitty ya es translucido de por si (background_opacity 0.95).
    # dim_inactive oscurece en vez de transparentar, que es la forma limpia de
    # que algo retroceda: transparentar deja el fondo colandose entre letras.
    active_opacity = 1.0
    inactive_opacity = 0.90
    dim_inactive = true
    dim_strength = 0.35
    dim_special = 0.5

    # La sombra dice "esto flota". Las ventanas en mosaico estan pegadas a la
    # rejilla y no la llevan (windowrule mas abajo); las flotantes y los
    # dialogos si. Es informacion sobre el plano, no adorno.
    shadow {
        enabled = true
        range = 28
        render_power = 4
        # color: lo pone pywal (~/.cache/wal/colors-hyprland.conf)
    }

    # https://wiki.hypr.land/Configuring/Variables/#blur
    # A size 3 / passes 1 el desenfoque no se veia. Lo que se beneficia es
    # lo que tiene transparencia de verdad: kitty va a 0.95 de opacidad, y por
    # ahi se ve el fondo. (Aqui ponia "los menus de rofi"; ya no hay.)
    # SI NOTAS TIRONES: vuelve a size = 3 / passes = 1 (era lo de antes).
    blur {
        enabled = true
        size = 6
        passes = 2
        noise = 0.01
        vibrancy = 0.1696
        vibrancy_darkness = 0.2
        popups = true
    }

    # Ley 5 — lo que ATRAVIESA la pantalla deja rastro. Hyprland 0.56 desenfoca
    # la ventana en la direccion en la que se mueve, mientras dura la animacion.
    # Aqui no pelea con el redondeo porque rounding = 0; con esquinas redondas
    # los dos shaders se excluyen y las esquinas saldrian rectas al viajar.
    # 'samples' son las copias que se promedian por el trayecto: mas muestras,
    # estela mas suave y mas cara. No alarga la estela.
    #
    # OJO, ESTE FICHERO ES EL RESPALDO: el interruptor de Ajustes > Apariencia
    # > Efectos escribe efectos.lua, que solo carga hyprland.lua. Si acabas
    # arrancando con este .conf, el interruptor no hace nada y manda esta linea.
    motion_blur {
        enabled = true
        samples = 7
    }
}

# https://wiki.hypr.land/Configuring/Variables/#animations
# ==========================================================================
#  MOVIMIENTO. La spec completa esta en ~/.config/motion-language.md
#  La velocidad va en DECISEGUNDOS: 1.3 = 130 ms. Son literalmente los mismos
#  cuatro numeros que los tokens de ~/.config/quickshell/Appearance.qml, para
#  que una ventana y el notch se muevan con el mismo pulso.
#
#     RESPUESTA  1.3 / 1.3    foco, zoom, color de borde
#     CONTENIDO  2.1 / 1.1    fundidos
#     PANEL      3.2 / 1.7    capas, mover ventanas
#     FORMA      4.4 / 2.2    abrir y cerrar una ventana
#     RECORRIDO  5.0          lo que ATRAVIESA la pantalla entera
#
#  Si algo se mueve aqui y no esta en esa escala, es un bug.
#
#  RECORRIDO es la excepcion: 500 ms, mas que FORMA, porque atravesar la
#  pantalla entera es literalmente el viaje mas largo que existe aqui y
#  cobrarlo al precio de abrir una ventana lo deja en nada.
#
#  ANTES ESTUVO EN 7.0 CON UNA CURVA 'frontal' (0.1,1)(0,1) copiada de end-4,
#  con la teoria de que una curva brutalmente adelantada deja usar duraciones
#  largas. En pantalla no funciono: se leia como un tiron seguido de una deriva
#  lenta, y diego lo describio como "raro". La leccion es que una curva tan
#  extrema no da "rapido Y con peso", da dos gestos pegados que no parecen el
#  mismo movimiento. Curva normal y duracion honesta.
# ==========================================================================
animations {
    enabled = yes

    # Tres curvas, y ninguna mas. La cuarta del sistema ('entra', la que
    # rebota) vive SOLO en Quickshell: aqui todo lo que se mueve es grande, y
    # por ley 5 lo grande no rebota. Un rebote a pantalla completa ensena el
    # borde de la pantalla y delata el truco.
    bezier = respuesta, 0.2,  0,    0,    1
    bezier = sale,      0.3,  0,    1,    1
    bezier = forma,     0.23, 1,    0.32, 1
    bezier = entra,     0.16, 1.3,  0.3,  1

    # 'apple': la curva de las transiciones de iOS/macOS. Arranca con un poco
    # de aceleracion en vez de salir disparada -- eso es lo que hace que parezca
    # que algo con masa se pone en marcha -- y despues frena durante casi todo
    # el recorrido. Es lo contrario de la 'frontal' que probe antes: aquella
    # metia el 90% del viaje en el primer pelin y el resto era deriva.
    bezier = apple,     0.32, 0.72, 0,    1

    animation = global, 1, 3.2, forma

    # Ley 6 — el movimiento tiene origen. Una ventana en mosaico se INFLA en
    # su hueco; no vuela desde un borde. El sitio ya lo habia decidido el
    # tiling, y entrar volando lo desmiente.
    animation = windows,     1, 4.4, entra, popin 78%
    animation = windowsIn,   1, 4.4, entra, popin 78%
    # Abrir recorre 22 puntos de escala (78->100) y cerrar solo 10 (100->90).
    # Deliberadamente asimetrico, no descuidado: abrir es un acontecimiento y
    # merece gesto; cerrar es un descarte y solo tiene que quitarse de en medio.
    # Antes cerraba al 82%, que es casi el mismo viaje que la apertura, y por
    # eso las dos cosas se parecian demasiado.
    animation = windowsOut,  1, 2.2, sale,  popin 90%
    animation = windowsMove, 1, 4.4, forma # el .lua vivo usa muelle 'mover' a 4.4; aqui solo se puede espejar el speed

    animation = fade,       1, 2.1, forma
    animation = fadeIn,     1, 2.1, forma
    animation = fadeOut,    1, 1.1, sale
    animation = fadeSwitch, 1, 2.1, forma
    animation = fadeShadow, 1, 2.1, forma

    # El foco. Con follow_mouse = 1 cambia constantemente, asi que la
    # atenuacion tiene que ser RESPUESTA: si tardara, el escritorio entero
    # temblaria cada vez que cruzas el raton por encima de una ventana.
    animation = fadeDim, 1, 1.3, respuesta

    # Capas (la barra y los paneles del notch, el lanzador, los avisos): se
    # FUNDEN. Nada a pantalla completa entra deslizandose, ensenaria el borde
    # por el que entra.
    animation = layers,        1, 3.2, forma, fade
    animation = layersIn,      1, 3.2, forma, fade
    animation = layersOut,     1, 1.7, sale,  fade
    animation = fadeLayersIn,  1, 3.2, forma
    animation = fadeLayersOut, 1, 1.7, sale

    # EL CAMBIO DE ESCRITORIO, ESTILO MACOS. Va entero: 'slide', no 'slidefade'.
    #
    # Aqui me equivoque dos veces seguidas y las dos por lo mismo. Primero puse
    # slidefade 30%, y luego encima le meti "paralaje": la que entra 20% y la
    # que sale 50%, razonando que dos planos a distinta distancia dan
    # profundidad. Sobre el papel es cierto. En pantalla lo que hace ese
    # porcentaje es RECORTAR EL VIAJE -- los escritorios se movian medio palmo
    # y el resto del trabajo lo hacia un fundido. Por eso se veia sutil: no es
    # que la animacion fuera floja, es que casi no habia desplazamiento.
    #
    # macOS no hace nada de eso y por eso se siente contundente: los dos
    # escritorios estan pegados como dos habitaciones contiguas y la pantalla
    # viaja el 100% de un lado al otro, a la vez, sin fundido ninguno. El
    # fundido es justo lo que delata que son dos imagenes superpuestas; sin el,
    # son un sitio del que te vas y otro al que llegas.
    #
    # Sin rebote: es un desplazamiento, y pasarse dejaria una franja de fondo
    # asomando por el lado contrario justo al final.
    #
    animation = workspaces,    1, 5.0, apple, slide
    animation = workspacesIn,  1, 5.0, apple, slide
    animation = workspacesOut, 1, 5.0, apple, slide
    animation = specialWorkspace,    1, 5.0, apple, slidevert
    animation = specialWorkspaceIn,  1, 5.0, apple, slidevert
    animation = specialWorkspaceOut, 1, 5.0, apple, slidevert

    # Invisible (border_size = 0), pero el nodo existe: que no se quede en la
    # velocidad de fabrica y desentone el dia que enciendas los bordes.
    animation = border, 1, 1.3, respuesta

    # borderangle / shadowangle / glowangle en 'loop' fuerzan repintado
    # continuo a 60 Hz aunque no se vean. En una Iris Xe eso es adorno que se
    # paga en fotogramas. Se quedan apagados (ley 7: nada se mueve porque si).

    animation = zoomFactor, 1, 1.3, respuesta
}


# --- Siempre tres escritorios ---
# Hyprland destruye un escritorio en cuanto se queda vacio, asi que la fila del
# indicador cambiaba de tamano sola: cerrabas la ultima ventana del 3 y el punto
# desaparecia. 'persistent' hace que existan aunque esten vacios.
#
# Se hace AQUI y no pintando puntos de mas en el shell a proposito: asi los tres
# escritorios existen de verdad, o sea que el punto es clicable y te lleva a el.
# Un punto pintado que no corresponde a nada seria un adorno mintiendo.
#
# Son un MINIMO, no un tope: si creas el 4 (Super+4, o mandando una ventana
# alli) aparece su punto solo, y desaparece al vaciarlo. Los tres primeros se
# quedan siempre.
#
# CON DOS PANTALLAS la numeracion sigue siendo unica para toda la sesion: los
# escritorios no se duplican por monitor. Hyprland le da uno a cada salida al
# arrancar y reparte los persistentes que sobran, asi que Super+2 desde la
# pantalla izquierda puede llevarte el FOCO a la derecha. Es de serie, no un
# fallo, y la barra ya solo pinta los escritorios de SU pantalla.
# Para repartirlos a mano se le anade el monitor (nombres de `hyprctl monitors`):
#   workspace = 1, persistent:true, monitor:DP-4
workspace = 1, persistent:true
workspace = 2, persistent:true
workspace = 3, persistent:true

# Ref https://wiki.hypr.land/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrule = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrule = rounding 0, floating:0, onworkspace:w[tv1]
# windowrule = bordersize 0, floating:0, onworkspace:f[1]
# windowrule = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hypr.land/Configuring/Dwindle-Layout/ for more
dwindle {
    # removed obsolete option: pseudotile = true
    preserve_split = true # You probably want this
}

# See https://wiki.hypr.land/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hypr.land/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(

    # De caelestia: se animan los CAMBIOS DE ESTADO, no la interaccion en vivo.
    # Arrastrar o redimensionar con el raton ya va 1:1 con tu mano, asi que
    # animarlo no aporta nada y en una iGPU es repintado continuo atado al
    # puntero (el camino mas caro que hay). No se pierde ni una pizca de
    # sensacion y se libera GPU justo cuando mas falta hace.
    animate_manual_resizes = false
    animate_mouse_windowdragging = false
    
}


#############
### INPUT ###
#############

# https://wiki.hypr.land/Configuring/Variables/#input
input {
    kb_layout = es
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = true
    }
}

# See https://wiki.hypr.land/Configuring/Gestures
gesture = 3, horizontal, workspace

# OJO si alguna vez se cae aqui: el gesto de 3 dedos hacia abajo que abre el
# calendario NO esta en este fichero. Vive solo en hyprland.lua, porque usa una
# tabla de callbacks (`action = { finish = function() ... end }`) que la sintaxis
# clasica de la .conf no tiene. No se ha escrito aqui una traduccion a ojo: sin
# poder probarla -haria falta retirar el .lua y recargar la sesion entera- seria
# una linea que parece que funciona y no funciona. Si vuelves a este fichero,
# ese gesto es lo unico que echaras en falta; el cuadro del centro de control
# sigue abriendo el calendario igual.

###################
### KEYBINDINGS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Captura de región con la pantalla CONGELADA: también fotografía lo que se
# cierra al perder el foco (el notch de Super+D, un menú, un desplegable).
bind = $mainMod SHIFT, S, exec, ~/.config/hypr/scripts/screenshot-region.sh
# Captura de pantalla COMPLETA instantánea (grim no roba foco → sí captura menús como rofi)
# Super+Shift+P es la tecla FIABLE; Print funciona si tu teclado emite ese keysym.
bind = $mainMod SHIFT, P, exec, ~/.config/hypr/scripts/screenshot-full.sh
bind = , Print, exec, ~/.config/hypr/scripts/screenshot-full.sh
# Bloquear la pantalla manualmente (hyprlock vía hypridle)
bind = $mainMod, L, exec, loginctl lock-session
bind = $mainMod SHIFT, V, global, quickshell:clipboard   # historial de portapapeles (modo # del lanzador del notch)
 

# Example binds, see https://wiki.hypr.land/Configuring/Binds/ for more
bind = $mainMod, Q, exec, uwsm app -- $terminal
bind = $mainMod, C, killactive, 
bind = $mainMod, M, exit,
bind = $mainMod, E, exec, uwsm app -- $fileManager
bind = $mainMod, I, exec, ~/ia-local/bin/ia-panel   # IA local: panel que conoce este sistema (modulo autocontenido)
bind = $mainMod, V, exec, ~/.config/hypr/scripts/toggle-float-centered.sh   # flotante al 60 %, centrada; segunda pulsacion vuelve al mosaico
bind = $mainMod, R, global, quickshell:launcher   # lanzador desplegado desde el notch
bind = $mainMod SHIFT, M, global, quickshell:media   # reproductor flotante
bind = $mainMod, N, global, quickshell:notchstyle   # alterna notch <-> isla
bind = $mainMod SHIFT, W, global, quickshell:wallpaper   # picker wallpaper (local+web)
bind = $mainMod, TAB, global, quickshell:overview   # mapa de escritorios en el notch (arrastrable)
bind = $mainMod, D, global, quickshell:notch   # centro de control en el notch
bind = $mainMod SHIFT, D, global, quickshell:system   # estado del equipo en el notch
bind = $mainMod SHIFT, E, global, quickshell:power   # menú de encendido en el notch
bind = $mainMod, comma, global, quickshell:settings   # app de Ajustes (como Cmd+, en macOS)
bind = $mainMod, A, global, quickshell:settings   # app de Ajustes (A de Ajustes, alias de Super+,)
bind = $mainMod, K, global, quickshell:keybinds   # mapa de atajos (Ajustes > Atajos), antes un rofi aparte

bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, layoutmsg, togglesplit # dwindle
bind = $mainMod, G, exec, ~/.config/quickshell/reload.sh   # recarga Quickshell (barra + notch)
bind = $mainMod, W, global, quickshell:bar   # ocultar/mostrar la barra superior

# Move focus with mainMod + SHIFT + arrow keys
bind = $mainMod SHIFT, left, movefocus, l
bind = $mainMod SHIFT, right, movefocus, r
bind = $mainMod SHIFT, up, movefocus, u
bind = $mainMod SHIFT, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
#bind = $mainMod, S, togglespecialworkspace, magic
#bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
# Volumen/brillo: el OSD lo pinta el notch (Quickshell). El volumen lo detecta
# solo vía Pipewire; el brillo lo cambia el propio notch por IPC para que no haya
# lag ni desfase de valor.
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1.4 @DEFAULT_AUDIO_SINK@ 2%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%-
bindl = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindl = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, qs ipc call notch bright up
bindel = ,XF86MonBrightnessDown, exec, qs ipc call notch bright down

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

# Al pulsar botón físico de power
bind = , XF86PowerOff, global, quickshell:power   # menú de encendido en el notch

# Mover/intercambiar ventana con Super + Flechas
bind = $mainMod, left, movewindow, l
bind = $mainMod, right, movewindow, r
bind = $mainMod, up, movewindow, u
bind = $mainMod, down, movewindow, d


bind = SUPER, O, exec, kitty --class pokemon-popup -e ~/.config/hypr/scripts/pokemon-popup.sh   # invoca un pokemon animado
##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hypr.land/Configuring/Window-Rules/ for more
# See https://wiki.hypr.land/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule
# windowrule = float,class:^(kitty)$,title:^(kitty)$

# --- App de Ajustes de Quickshell (SettingsWindow.qml) ---
# Es la única ventana de verdad (xdg-toplevel) del shell: class org.quickshell,
# título "Ajustes"; el resto del shell son capas (layer-shell) y no pasan por
# aquí. Sin estas reglas se abría TILED y ocupaba media pantalla (952x1038).
# La queremos siempre flotante, contenida y centrada. Si algún día la quieres
# anclada, Super+V la desancla/ancla a mano sin tocar la config.
# El tamaño va también en el QML (implicitWidth/Height) para que no haya un
# fogonazo grande antes de que Hyprland la redimensione. No bajar de 780 de
# ancho: la ventana tiene mínimo propio ahí.
# OJO con la sintaxis: Hyprland 0.53 rehízo las windowrule y la de toda la vida
# ("windowrule = float, class:^(x)$") ya NO vale — da "invalid field float:
# missing a value". Ahora los filtros son campos match:* y los efectos llevan
# valor. El ejemplo comentado de arriba es de la sintaxis vieja.
windowrule {
    name = ajustes-quickshell
    match:class = ^(org\.quickshell)$
    match:title = ^(Ajustes)$

    float = true
    size = 900 620
    center = true
}

# --- Ley 9: una animacion, un dueno ---
# El compositor anima las superficies que APARECEN; la app anima lo de dentro.
#
# La barra/notch de Quickshell es una capa PERMANENTE que se redimensiona sola
# y que ya se anima ella misma (tokens de Appearance.qml). Si ademas la animara
# Hyprland, se encadenarian dos animaciones sobre el mismo gesto y se veria un
# borron. Aqui se le cede el mando a Quickshell.
layerrule = no_anim on, match:namespace ^(quickshell:bar)$

# --- Ley 6 aplicada a las superficies del shell: cada una entra por donde le
# --- corresponde, en vez de un fundido generico para todas.
#
# Esto solo es posible desde que las tres declaran WlrLayershell.namespace (una
# linea en cada .qml). Antes no habia nada a lo que agarrarse y se comian el
# 'layers' global.
#
# El criterio de cada una sale de la ley 9 (una animacion, un dueno): hay que
# mirar QUE anima ya la app por dentro y darle al compositor solo lo que quede
# libre. Si los dos animan lo mismo, los dos fundidos se multiplican y el
# resultado no es el doble de bonito, es que llega tarde.
#
# media: vive pegado al borde de abajo, asi que SUBE desde ahi. 'slide' mueve
# posicion y nada mas -- no funde -- y la tarjeta por dentro ya se encarga de
# la escala y la opacidad. Canales distintos, no se pisan.
layerrule = animation slide bottom, match:namespace ^(quickshell:media)$

# wallpaper: ocupa la pantalla entera, asi que deslizarla ensenaria el borde
# por el que entra. Y ademas su 'stage' ya se funde solo (animMed, OutCubic).
# El compositor no tiene nada que aportar aqui: que se aparte.
layerrule = no_anim on, match:namespace ^(quickshell:wallpaper)$

# overview: esta regla se retira el 6-ago-2026 y NO hay que volver a ponerla.
# El overview ya no es una superficie propia: es una cara del notch, o sea que
# vive dentro de quickshell:bar y su entrada la hace el morfeo de la forma del
# propio notch. Una regla de capa aqui no tendria a que aplicarse.

# Aqui habia dos layerrule (blur + ignore_alpha 0.5) sobre la capa de rofi, que
# era lo unico con transparencia real. Se van con rofi el 19-08-2026: una regla
# que apunta a una capa que ya nadie crea no hace nada salvo hacerte creer que
# rofi sigue en el escritorio.
# (Por si vuelve a hacer falta una regla de capa: la sintaxis posicional de
# antes -- "layerrule = blur, quickshell:bar" -- no la rompio 0.56 sino la
# reescritura de reglas de 0.53; la forma buena hoy es con match:.)

# La sombra dice "esto es lo que estas mirando": SOLO la ventana enfocada la
# proyecta. Junto con dim_inactive, el foco deja de ser un matiz y pasa a ser
# lo primero que ve el ojo al entrar en la pantalla, sin una sola linea de
# borde. (Antes esto era "solo las flotantes", que en un tiling es casi nunca:
# la regla era correcta sobre el papel y en la practica no se veia jamas.)
windowrule = no_shadow on, match:focus 0



# pywal: bordes dinámicos del wallpaper (revertir = comenta la línea source)
source = ~/.cache/wal/colors-hyprland.conf

# Selector de fondos con re-tematizado pywal (Super+Shift+W)

# --- Quick wins ---
bind = $mainMod CTRL, S, exec, ~/.config/hypr/scripts/screenshot-annotate.sh   # anotar captura (satty)
bind = $mainMod SHIFT, C, exec, ~/.config/hypr/scripts/colorpicker.sh          # cuentagotas (hyprpicker)
bind = $mainMod SHIFT, N, exec, ~/.config/hypr/scripts/hyprsunset-toggle.sh    # luz nocturna toggle

# --- Signature: menú/ocr ---
bind = $mainMod ALT, Space, global, quickshell:actions   # menú de comandos (modo > del lanzador del notch)
bind = $mainMod CTRL, T, exec, ~/.config/hypr/scripts/ocr.sh              # OCR de pantalla

# --- Grabar pantalla (wf-recorder) ---
bind = $mainMod CTRL, R, exec, ~/.config/hypr/scripts/screenrecord.sh
